# 2026-09-19

## copilot-server-agent 并发与多租户安全一轮整改

按 review 的 P0/P1 清单改完（review 结论：不同 session 已隔离，但同 session 多 request 未串行 + ownership 不可信）。

关键改动：
- **streaming lock**：SSE chat 改为在 `withSessionLock` 内 `sendAndWait()`（`send()` 只入队就返回，锁不到 agent loop）；事件监听器也移进锁内，避免并发请求互收 delta。新增 `sseFail` 收尾 + 客户端断开 `session.abort()` 当前 turn。
- **attach lock**：`getOrResumeSession` 加 `sessionAttachLocks`，解决并发首访双附着。
- **lifecycle lock**：disconnect / permanent delete 走同一 session lock；delete 用 `disconnectUnlocked` 避免重入死锁。
- **Session Registry**（新 `server/src/services/session-registry.ts`）：持久 JSON（原子写 + 串行 mutate），替代内存 owners Map；`assertOwnership` 统一归属校验，仅单租户下允许 legacy 认领。listSessions 按 owner 过滤。
- **工具授权**（新 `server/src/services/tool-policy.ts`）：取代 `approveAll`。read 放行 / write 限 workspace / shell 按 `COPILOT_BASH_POLICY`(默认 workspace) / MCP 限本次启用 server / URL 过 SSRF + allowlist / 未知 kind 拒绝。另加强制 `onPreToolUse` workspace 守卫（写类工具）。
- 路径判定坑：macOS `/var` → `/private/var` symlink，必须同时比对字面路径与 realpath，否则 workspace 内路径会被误判为越界。
- K8s：拆两个 PVC（去掉 subPath）、加 `fsGroup: 1000`、API 容器挂 runtime-state 卷放 registry。
- 版本：SDK 精确 pin 1.0.14，新增 `scripts/check-versions.mjs`（四处一致校验）与 `scripts/verify-agent-tools.mjs`（只认 tool.execution_start，防 #2356）。

验证：typecheck/build 通过；冒烟确认 create→chat(SSE error 帧正常收尾)→list→403(u2 访问 u1 会话)→permanent delete(registry 清空)。

## 注释/文档措辞约定（用户偏好）

代码注释与文档不写「第一版/第二版/第一阶段/下一阶段/Level 1」这类版本演进字眼，只描述当前实现与当前约束。
需要表达"换场景怎么办"时，写成条件句（例：「多副本部署需换分布式锁」）而不是「下一阶段升级为…」。

## health 徽章 "disconnected" 语义修正

现象：刚打开页面徽章显示 `Copilot: disconnected`，但功能正常。
根因：`SessionService.getStatus()` 把「client 懒加载未创建」等同于 disconnected；`/api/health` 不碰 runtime（避免探针风暴），所以未发起会话前永远是 disconnected。
改法（用户选「两者都做」）：
- `getStatus()` 三态：`connected` / `idle` / `error`（idle = client 未创建，中性色，不是故障）；types.ts 与 client Health 类型同步。
- 新增 `SessionService.warmup()` + `COPILOT_WARMUP`（默认 true），server 启动后后台建连，不阻塞监听、失败只记日志。
- `HealthBadge` 三态渲染：connected 绿 / idle 灰（「未启动（首次会话时连接）」）/ error 黄 + 原因。
- `/api/debug` 配置块补充 registryPath / trustIdentityHeaders / bashPolicy。
验证：3s 内 healthy 变 connected 且日志「预热完成」；`COPILOT_RUNTIME_URL` 指向死端口时显示 error + ECONNREFUSED 原因。


## Execution Record / 工具证据 / usage telemetry（借鉴 a2a-wrapper 的 P0 部分）

新增 `server/src/execution/`（store/usage/redact）+ `server/src/services/tool-evidence.ts`：
- execution = 一次 chat turn。session ≠ execution ≠ tool call ≠ LLM call；审计/usage/取消/终态全挂 `executionId`（`ex_<uuid>`），hook 事件环每条自动补 `executionId`（记录时按 session 的 active execution 解析）。
- usage：`LlmUsageAccumulator` 是 execution-local（绝不挂全局），监听 `assistant.usage`（token/duration/model/cost）+ `session.usage_info`（tokenLimit=contextWindow）。
- 工具证据：强制 hook `onPreToolUse`（开条：toolCallId/脱敏参数/守卫裁决）+ `onPostToolUse` / `onPostToolUseFailure`（收口：结果/错误/耗时）。SDK 的 pre hook input 没有 toolCallId，用 `${executionId}-t{n}` 自造 + 按 session 的 LIFO 栈配对。
- 脱敏截断：`redact.ts` 按 key（token/api_key/authorization/password/secret…）与值形态（Bearer/sk-/ghp_/JWT…）替换后序列化截断，只报长度。
- 新端点 `GET /api/executions`、`GET /api/executions/:id`（admin）；SSE 首个帧 `event: execution`。
- 配置：`COPILOT_EVIDENCE_MAX_CHARS` / `COPILOT_MAX_TRACKED_EXECUTIONS` / `COPILOT_MAX_TOOL_CALLS`。
- 测试：`server/test/*`（`npm run test`，node --import tsx --test）+ `server/tsconfig.test.json` 让测试也过类型检查；config smoke 用子进程加载验证非法值必须启动失败。

### 踩坑（重要）
`onPermissionRequest` 放行必须返回 `{ kind: 'approve-once' }`（与 SDK `approveAll` 一致）。类型联合里也有 `approved`，但那是状态回执，runtime 在 hook 响应路径上不认 —— 用了会报
`permission orchestrator internal error: unexpected user permission response: approved`，表现是「工具调用一律失败」。

## Durable Execution + Human-in-the-loop + Approval（按架构建议落地）

派生自 review 的建议，明确不引入 Redis/Temporal/BPMN/Kafka，PostgreSQL 统一持久化，LLM 不定义授权边界，审批批准的是 intent（不是 HTTP 请求）。

新增模块：
- `db/pool.ts`（PG 连接池，`pg` 为可选依赖，惰性 require；未配 `DATABASE_URL` 走内存）+ `db/migrations/001_agent_execution.sql`（5 张表）。
- `execution/`：`types.ts`（状态机 created/running/waiting_for_input/waiting_for_approval/resuming/completed/failed/cancelled/rejected/expired）、`repository.ts` + memory/postgres 双实现、`hash.ts`（actionHash sha256）、`events.ts`、`execution-service.ts`（生命周期编排）。
- `human-tasks/`（input + approval 两类任务、委派留痕）、`approval/`（ANY/ALL/N_OF_M/SEQUENTIAL 策略 + SoD + `unique(task_id, approver_id)` 一人一票）、`actions/`（server-controlled 动作：propose → approve → hash/resourceVersion 复核 → execute）。
- `agent/`（agent-runner / agent-events / agent-context 收口 SDK 事件）；`services/`（concurrency 信号量、task-sweeper TTL 过期、principal）；`routes/` 拆分为 shared/sessions/executions/human-tasks/meta，`api.ts` 只做挂载。
- `principal` 从认证身份取 approverId，**不接受前端传入**（授权边界在 server）。
- `docs/architecture.md`（11 节：分层 / 对象关系 / 状态机 / HITL / 授权三层 / 并发锁 / 审计分层 / 数据模型 / API / 目录 / 明确不做）。

## 完整性核查：PG 路径两处列缺失（已修）

`git status` 干净（work 已 commit），typecheck / build / test 通过。逐列比对 PG 仓储与 DDL 后发现两个只在 PostgreSQL durable 路径暴露的缺陷（内存实现无影响，因此测试不报）：

| 缺陷 | 后果 |
|---|---|
| `agent_execution.content_chars`：`toRecord` 读、DDL 与 `COLUMNS` 都没有 | PG 下 `contentChars` 静默丢失 |
| `human_task.input_values`：`toTask` 读 + `update()` 写，DDL 没有 | 提交人工输入报 `column does not exist`，resume 链路断在 `/resuming` 之前 |

修复：DDL 补两列 + 末尾加幂等 `alter table ... add column if not exists`（`create table if not exists` 不会给已建库补列）；`COLUMNS`/`toValues` 补 `content_chars`；`docs/architecture.md` 字段表同步。

新增 `server/test/schema.test.ts`：解析 DDL 得表→列，扫 PG 实现里的行属性读取（`r.snake_case`）与 DDL 求差，另校验 5 张表齐全、≥3 处级联删除、一人一票唯一约束。**已验证该测试在缺列时确实失败**。误报率为 0（扫出的标识符全是真实列）。

## 待办（已知缺口，未实现）

- 前端只有 execution 审计查询（`GET /api/executions/:id`），**没有审批 / 人工任务的 UI**：`/api/human-tasks` 那组端点目前只能靠 API 调用。

## DSN 校验与部署示例收尾

- `config.ts` 新增 `parseDatabaseUrl`：非空必须是 `postgres://` / `postgresql://`，占位值或错 scheme **启动即失败**（沿用「非法配置不静默回退」的既有约定）。空=内存模式。
- `k8s/secret.example.yaml` 补 durable 模式的 `database-url` + 迁移命令说明；**故意不写占位值** —— key 存在但值非法会让服务进 PG 模式后连不上，比 key 缺失更糟。
- `config.test.ts` 用子进程加载验证：留空 / 合法 / `REPLACE_ME` / `mysql://` 四种。

## 坑：并行 Edit 会互相覆盖

同一消息里对**同一文件**发两个 `Edit`，第二个基于旧快照写回，会**静默丢掉第一个改动**（工具仍报 success）。本次踩到一次（DDL 的 `content_chars` 被吞），靠测试才发现。合并到一次 Edit，或改完立刻 grep 复核。
