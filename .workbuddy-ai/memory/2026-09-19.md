# 2026-09-19

## Skill Flow / WorkflowRunner 三项加固（基于代码评审）

基线 commit `c43e74c`。评审结论：架构方向没问题，不需要推翻重做，改 failure semantics 与 step durability。

### 1. P0 runner 异常收尾（`server/src/workflow/runner.ts`）
- `runDetached()` 原来只 `console.error`，未预期异常会留下永远 `running` 的 execution。
- 改为：catch → `executions.get` → 非终态则 `executions.fail(id, err)`；连落 failed 都失败再 log。
- 需要 `import { isTerminal } from '../execution/types.js'`。
- 注意：`runSubagent`/`runGate` 自己 catch 了节点内失败（那是可路由出口），所以这条路径测试里不容易碰到，必须专门写用例（monkey-patch `executions.updateWorkflowState` 抛错来模拟）。

### 2. step durability：`WorkflowState.stepStatus`
- 新增 `WorkflowStepStatus = 'pending' | 'running' | 'waiting' | 'completed'`（optional，老数据按 pending）。
- `loop()` 里**先落 `running` 再执行节点**，执行完落 `{current: next, stepStatus: 'pending'}`；
  review/action 等人工落 `waiting`；`@stop`/`@end` 落 `completed`。
- 新增 `admitInterruptedStep()`：恢复时若 `stepStatus === 'running'` →
  `REPLAY_SAFE_NODE_TYPES = {agent, gate}` 允许重放（记 `workflow.step.interrupted` 审计）；
  `@action` **不重放**，落 failed 交人工核对（幂等键防不了"外部已生效但本地没记上"）。
- 新事件类型 `EVT.workflowStepInterrupted = 'workflow.step.interrupted'`。
- `workflow_state` 是 JSON blob 列，加字段不需要迁移。

### 3. validator 禁止同一出口多条 route
- 新增 `checkDuplicateOutcomes()`，code `route-outcome-duplicate`，节点与 `@flow` 的 `start` 都查。
- 原因：`findRoute()` 是 `routes.find(...)`，第二条静默失效。
- parser 已按 `on->to` 去重，所以"完全重复的同一条"不算歧义。

### 4. `@subagent` → `@agent`（旧名保留为别名）
- 语义澄清：它跑的是**当前 session 的又一次 agent turn**（`runExecutionTurn`），不是真正的 subagent 委派。
- parser 用 `BLOCK_ALIASES = { subagent: 'agent' }` 归一化，下游只认 `agent`。
- `FlowNodeType` 里 `'subagent'` 已移除；`runSubagent` 改名 `runAgent`。
- **不要动** `subagent.*` SDK 生命周期事件（`agent-runner.ts` / `agent-events.ts` / `Chat.tsx` / SSE `subagent` 帧）—— 那是另一回事。

### 验证
- `npm --prefix server run typecheck` ✅
- `npm --prefix server test` → 138/138 ✅（新增 6 个用例：别名兼容、stepStatus 写入顺序、@agent 重放、@action 不重放、runDetached 收尾、重复出口）

## 项目约定备忘
- 测试命令：`npm --prefix server test`（`node --import tsx --test test/*.test.ts`），无 vitest/jest。
- `server/dist/`、`client/dist/`、`/dist/` 都在 .gitignore 里，改了 src 不需要提交 dist。
- 工作流源码集中在 `server/src/workflow/{runner,registry,types}.ts` + `server/src/skills/flow-{parser,validator}.ts`。

---

## 业务角色模型：Role / Entra Group / 授权解析（按设计提案全量实现）

用户决策：**按提案全量实现** + **group → 角色解析放在网关**（服务端不调 Microsoft Graph）。

### 新增 `server/src/identity/`
- `business-roles.ts`：RoleRegistry。`BusinessRole { id, name, groups[], match: ANY|ALL }`，
  `resolveRolesForGroups()` 是**同步纯函数**（无网络/无 token）。
  内置角色只登记 id 不登记 group；映射由 `COPILOT_BUSINESS_ROLES`（JSON 数组）配置，
  配错启动即失败。没配 groups 的角色谁都拿不到（fail-closed）。
- `authorization-service.ts`：`resolveRoles(principal)` = `principal.roles` ∪ group 映射，
  同步、去重、排序。**同步是刻意的** —— 它被 `HumanTaskService.withTaskLock()` 临界区包着。

### 身份
- `Principal.groups?: string[]`（Entra group **object ID**），来自 `x-user-groups`（网关注入，最多 200 条）。
- `principalFromHeaders` 未开启 `COPILOT_TRUST_IDENTITY_HEADERS` 时 groups=[]。

### 授权接线（两处，都要用解析后的角色）
- `ApprovalService.assertEligible`：拿 `resolveRoles(principal)` 比 `policy.eligibleRoles`。
- `HumanTaskService.actorRoles()` → `isAssignee` / `list`（"我的任务"可见性）。
  **漏了后者 = 走 AD group 的人看不到自己的待办。**
- `isAssignee(task, actor)` 签名从 `Principal` 改成 `{ userId, roles }`（已解析角色）。

### SKILL.md 保留属性
- 只有 `@review`（role/strategy/required/exclude）与 `@action`（role）支持，**必须写在正文最前面**。
- parser 只做词法（属性区切分 + 名字检查），validator 做值校验。
- 属性区判定："最前面连续一段 `name: value`"；段内已认领到属性时，认不出的名字报
  `block-attr-unknown`（抓 `roles:` 拼错），否则整段当正文（`Note: ...` 开头不误伤）。
- 新 issue code：`block-attr-unsupported` / `block-attr-unknown` / `block-attr-duplicate` /
  `attr-invalid` / `role-missing` / `review-missing-role`。
- `role` 形态校验在**小写化之前**做（`BUSINESS_ROLE_ID` 要求全小写），
  这样 `APP-FIL-Compliance-Reviewer` 这类 AD 组名会被直接拒掉。

### 语义要点
- `@review role:` **覆盖**注册表 `eligibleRoles`；两边都没有 → `review-missing-role`。
- `@action role:` 与该 actionType 的 `ApprovalPolicy.eligibleRoles` **取交集**，空交集 → deny。
  只能收窄不能放宽；声明了 role 的动作**不走 `COPILOT_AUTO_APPROVE_ACTIONS`**。
- 内置角色 id 改名：`compliance-reviewer` → `compliance.reviewer`，`investment-reviewer` → `investment.reviewer`。

### 验证
`npm --prefix server run typecheck` ✅ / `npm --prefix server test` → **159/159** ✅（新增 21 个用例）

---

## 第三轮：安全加固（同日，接上一节）

⚠️ **上一节"`@review role:` 覆盖注册表"已作废** —— 那是可以放宽授权的漏洞，本轮改成交集。

### 1. 四个属性一律只能更严（`@review`）
- 注册表 `FlowReview` 成为**权威基策略**：`eligibleRoles` 必填，新增 `allowInitiator`（默认 false）。
- `role` → 与基策略取**交集**；`strategy` → 只能 ANY→ALL；`required` → 只能 ≥ 基策略；
  `exclude: none` → 基策略不允许自批时报错。
- 新 issue code：`role-not-allowed` / `attr-widens`。
- 运行时 `reviewPolicyFor()` 再 clamp 一次（不信任校验器，fail-closed）。
- 注册表查找回调从 `reviewRoles()` 换成 `reviewPolicy()`（返回 `ReviewBasePolicy`），
  `undefined` = 未登记，空 `eligibleRoles` = 登记了但没人有资格（→ `review-missing-role`）。

### 2. `@gate` 出口静态声明
- `FlowGate.outcomes` **必填**（注册时校验非空）；`flowRegistryLookup.gateOutcomes()`。
- 新 issue code：`gate-outcome-unrouted`（声明了但没 route）/ `gate-outcome-unknown`（route 了但没声明）。

### 3. `@agent` 能力边界（`workflow/capability.ts`，新文件）
- 上限 `COPILOT_WORKFLOW_AGENT_TOOLS`（默认 `read,write,url`，**不含 mcp/shell** —— 那两条是绕开
  `@action` 审批的路径）。`@agent tools:` 只能收窄（`agent-tools-widens`）。
- 进程内 `Map<sessionId, AgentCapability>`，`tool-policy.ts` 的 `ToolPolicyContext.capability`
  是**回调**（建会话时还不知道节点），权限请求时现取；第 0 层判定，只收窄不替代原检查。
- `session-service.ts` 的 `policyCtx` 注入 `capability: () => agentCapabilityFor(sessionId)`。

### 4. `@agent` 完成契约
- `@agent output: <id>` + 服务端 `registerFlowOutput({name, description, validate({content})})`。
- 契约没过 → `fail` 出口（可路由）。内置示例 `non-empty`。
- `COPILOT_WORKFLOW_REQUIRE_AGENT_OUTPUT=true` 时强制每个 `@agent` 都声明（`agent-output-missing`），默认关。
- 新 issue code：`registry-missing-output` / `agent-output-missing` / `agent-tools-widens`。

### 5. 单写者 / CAS
- 新列 `agent_execution.workflow_version integer not null default 0`
  （migration `004_workflow_single_writer.sql` + `sqlite-schema.ts` CREATE TABLE + COLUMN_UPGRADES）。
- ⚠️ **`workflow_version` 不在 `sql-repository.ts` 的 `COLUMNS` 里** —— 只由 CAS 语句抬高，
  否则一次 `addUsage()` 的整行 upsert 就会把它写回旧值。
- 新仓储方法 `compareAndSwapWorkflowState(executionId, expectedVersion, workflow)`；
  `ExecutionService.updateWorkflowState(id, state, expectedVersion?)` 返回
  `WorkflowStateWrite = {ok:true,version} | {ok:false,conflict:true,current?}`。
- runner 内 `StateWriter` 串版本号；冲突 → 留痕 `workflow.write.conflict` + **停止推进**，
  **绝不落 failed**（否则会打死别人正在跑的流程）。
- 进程内 `withExecutionLock(executionId, fn)`（promise 链，不是"忙就丢弃"）。

### 6. 人工任务回调绑定
- 四件事全对才续跑：`waitingTaskId === task.taskId`、`state.current === marker.nodeId`、
  `stepStatus === 'waiting'`、`rec.status === 'waiting_for_approval'`。
- 任一条不成立 → `workflow.resume.rejected` 事件 + 不动状态。
- **顺带修掉一个 bug**：workflow 任务过期/取消走的是编排器回调（不经过
  `ExecutionService.onHumanTaskResolved`），原实现只写事件不落终态 → execution 永远卡在
  waiting_for_approval。新增 `settleUnresumable()` 落 expired/cancelled 终态。

### 7. 接口边界拆分
- `workflow/definition-provider.ts`（新）：`FlowDefinitionProvider` + `SkillFileDefinitionProvider`
  （`load` / `reload`，`reload` 校验 sourceHash，issue code `skill-changed`）。
- `workflow/runtime.ts`（新）：`FlowNodeRuntime` + `DefaultFlowNodeRuntime`
  （`runAgent` / `runGate` / `runAction` / `openReview`），能力边界与完成契约在这里落地。
- `runner.ts` 只剩状态机：推进 / 落库 / 路由 / 人工任务接回。
- `WorkflowTurnRunner` 类型从 runner.ts 移到 runtime.ts（runner 仍 re-export）。

### 验证
`npx tsc --noEmit` ✅ / `npm test` → **179/179** ✅
（新增 `test/workflow-capability.test.ts`，flow-validator 与 workflow 各补一批用例）

### 文档
`docs/skill-flow.md` 更新：`@agent`（output/tools）、`@gate`（outcomes）、`@review`（四属性表）、
`@action`（role 交集），新增第 17 节"运行时加固"。

---

## Skill Flow 第四轮加固（基线 `88a190d`）

评审的 10 条全做完，核心是**把"不确定的中间态"消掉**。19 改 + 1 新测试文件，
1340 insertions / 166 deletions。`tsc --noEmit`（src + test）✅ / `npm test` → **196/196** ✅（179 → 196）。

### 1. 🔑 等待态落库顺序反转（本轮最重要）
**旧顺序**：`create HumanTask` → `transition(execution = waiting_for_approval)` → CAS 写
`workflow.stepStatus = waiting`。中间崩 → `execution = waiting_for_approval` + `workflow = running`
+ 任务已建但 `waitingTaskId` 没落。这个态**不可判定**：没有任何字段能说明"到底该不该续跑"。

**新顺序**（`runActionStep()` 与 `pauseForReview()` 同步改）：
```
create HumanTask → CAS 写 workflow = waiting + waitingTaskId → transition(execution = waiting_for_approval) → 事件
```
崩在任何一步都**可判定**：`stepStatus === 'waiting'` 但 `waitingTaskId` 缺 → 直接 failed；
`stepStatus === 'waiting'` + 有 `waitingTaskId` → `run()` 开头的 `reconcileWaiting()` 补
`running → waiting_for_approval`（事件 `workflow.waiting.reconciled`）。

代价：可能留下**孤儿人工任务**（任务建了、状态没落）。接受这个代价换可判定性 —— 孤儿任务靠
过期 + 四重绑定检查自愈，不写专门的清理器。CAS 冲突时走 `orphanTaskNote()` 记
`workflow.orphan_task`（**不要**复用 `workflow.write.conflict`，语义不同）。

契约：`runtime.runAction()` **只建任务，绝不碰 execution 状态**（单写者原则）。
`ExecutionService.proposeAction()` 新增 `workflow.deferWaitingTransition?: boolean` ——
置位时只建任务 + 发 `approvalRequired`/`humanTaskCreated` 事件就 return，不 transition、
不发 `waitingForApproval`（那是调用方的事）。

连带：`@review` 加进**不可重放**集合（原 `REPLAY_SAFE_NODE_TYPES` 只有 agent/gate）。
理由：`@review` 的"已完成产物"是一个人工任务，若崩在 `waitingTaskId` 落库前，
这个产物在持久化状态里**不可见** → 重放会建第二个任务。`@review` 中断的提示文案改成指向
"我的任务"（类型感知），因为审批入口在前端。

### 2. `COPILOT_WORKFLOW_REQUIRE_AGENT_OUTPUT` 默认翻成 true
`@agent output:` 从"可选"变"默认必填"（`agent-output-missing`）。

### 3. 🔒 mcp/shell 从"配置默认"升格为"代码硬禁"
`workflow/types.ts` 新增 `WORKFLOW_AGENT_ALLOWED_KINDS = ['read','write','url']` +
`WORKFLOW_AGENT_FORBIDDEN_KINDS` + `isWorkflowAgentKind()`。
`COPILOT_WORKFLOW_AGENT_TOOLS=mcp` **不再有任何效果**：
- `parseAgentTools()` 直接过滤掉 forbidden kind（全被滤掉则回退默认）；
- `resolveAgentTools()` 再与硬上限取交集（真正的咽喉）；
- `flow-validator.ts` 新增**无条件**检查 → `agent-tools-forbidden`
  （与"超出部署上限"的 `agent-tools-widens` **分开两个 code**，否则硬禁看起来像软禁）。
文档明确写"这不是配置项"，防止有人用 env var "修"。

### 4. `strategy: ALL` 必须真的全员
单靠"只能更严"不够 —— `base ANY + strategy ALL + required 1` 组合方向是对的（ANY→ALL 更严），
但结果是 `ALL` 只要求 1 票 = 和 `ANY` 一样，**名字在撒谎**。
- `types.ts` 新增唯一共享定义 `requiredVotes({strategy, requiredCount, roleCount})`
  = `strategy === 'ALL' ? max(requiredCount, roleCount) : requiredCount`。
- `registry.ts` `registerFlowReview()` 启动即校验：`eligibleRoles.length > 0`、
  `requiredCount` 整数 ≥ 1、`ALL ⇒ requiredVotes === requiredCount`（矛盾直接 throw）。
- `registry.ts` `reviewBasePolicy()` 的默认值也改了：`ALL` 缺省 `requiredCount` 时默认
  = 角色数（**不是 1**），且**不做 clamp**（clamp 掉就查不出矛盾了）。
- `flow-validator.ts` `checkNarrowing()` 按**生效后**的 strategy/roles/required 算，新增
  `review-all-required`。
- `runner.ts` `reviewPolicyFor()` 用同一个 helper clamp，并 **export** 出来给单测直接用。
- 关键：比的是**生效后**的角色集 —— 若 `role:` 把 3 个收成 1 个，`ALL + required 1` 是合法的。

### 5. 注册表自洽 + 顺带修两个潜伏 bug
- `registerFlowGate()`：`outcomes` 必须非空、去重、归一化小写。
- 新增 `norm` + `requireName(kind, name)`：**注册键统一小写**。
  原实现只在查找侧归一化（`get(name.toLowerCase())`），注册侧用原名 → 任何含大写的注册
  永远查不到（表现为"服务端说没注册"，不是崩）。`registerFlowAction`/`registerFlowOutput` 同改。
- `runGateStep()` 也归一化 `result.outcome.trim().toLowerCase()` —— 否则 gate 实现返回 `'PASS'`
  永远匹配不上小写 route。

### 6. 能力（capability）必须绑到某次执行
`AgentCapability` 加 `executionId`；`clearAgentCapability(sessionId, {executionId, nodeId})`
两个字段都要对上。`tool-policy.ts` 的 Layer-0 先比 `ctx.activeExecution()` 与
`capability.executionId`，**不匹配或 undefined 一律 deny**（fail-closed）。
`session-service.ts` 的 `policyCtx` 补 `activeExecution: () => executionContext.current(sessionId)`。
⚠️ 这里**故意不**"忽略能力、退回 session 策略" —— 退回等于把 mcp/shell 还给 agent。

### 7. 单次读取：定义/hash 必须同一份字节
`skills/index.ts` `loadSkill()` 重写：每个候选文件**只读一次**，匹配 + frontmatter + hash 全来自
这一次读。`definition-provider.ts` 去掉 `findSkill` 改用 `loadSkill`。
否则 `find(path)` 读一次匹配、再读一次胜出者 → "定义来自第 1 次读、hash 来自第 2 次读"，
中间文件被换掉就出现"hash 说没变、但定义从没校验过"。
`runner.ts` `runAgentStep()` 里多余的 `findSkill(node.id)` 也删了（第二套解析路径 = 执行期和
校验期可能不一致）。

### 验证与文档
`docs/skill-flow.md`：`output` 改成"默认必填"；`tools` 区分硬禁 vs 部署上限；
`@review` 加"`strategy: ALL` 必须真的全员"小节 + 场景表；第 17 节改名为
**"运行时加固：不确定性的来源"**，新增 17.6 等待态顺序 / 17.7 能力属于某次执行 /
17.8 单次读取 / 17.9 注册表自洽。

### 遗留
- 仓库根出现**未跟踪**的 `skills/bpmn-to-flow/SKILL.md`（约 17:20 创建，frontmatter 畸形：
  `---` 后有空行）。不是我建的、也不是 server 技能目录，**没动**，待确认。
- 复用型 skill `~/.workbuddy-ai/skills/narrow-only-policy-attributes/SKILL.md` 已补 4 节：
  "narrow-only 必要但不充分"、"禁止值属于代码不属于配置默认"、"注册键两侧都要归一化"、
  "版本标识：hash 和 parse 必须同一份字节"，测试清单同步扩充。

---

## 第四轮自查补丁（同一轮，199/199）

自己对第四轮 diff 做了一遍评审，找到一个**第四轮没闭合**的洞，外加一个**既有地雷**。

### A. `reconcileWaiting()` 只看 durable 字段 → 会永久卡死（第四轮引入）
`running` + `stepStatus = waiting` + `waitingTaskId = T` 这个形状有**两个来源**：
- 甲：崩在"落 waiting"与"迁移 execution"之间 → T 还 open，该等它；
- 乙：崩在 `resumeInto()` 的"`ensureRunning` 推出等待态"与"CAS 写 `current = next`"之间
  → T **已经被处理过**（那次续跑正是在处理它）。

第四轮的 `reconcileWaiting` 一律按甲处理 → 把 execution 推回 `waiting_for_approval`
等一个**永不再来的回调**；而 `ExecutionService.complete()` 在 `waiting_for_approval` 下是
**静默 return**，所以流程**永久卡住**，超时也救不回来（超时只对 open 任务产生回调）。

修法：`reconcileWaiting` 回查 `humanTasks.get(taskId)`，按任务**实时终态**分派：
- `open` → 甲，补迁移；
- 已收敛 → 摆回 `waiting_for_approval`（满足四重绑定）后 `resumeFromTask()` 原样重放；
- 查不到 → 落 failed，不瞎猜。

顺带覆盖了"**停机期间任务自然过期/被取消、回调没送达**"—— 之前也是永久卡死。

### B. `failWorkflow()` 在等待态会抛"非法状态迁移"（既有 bug）
`ALLOWED_TRANSITIONS` 里 `waiting_for_approval` / `waiting_for_input` 只有
`resuming / rejected / cancelled / expired`，**没有 `failed`**。
所以等待态调 `failWorkflow()` → `fail()` → `transition()` 抛异常；而它跑在**收尾路径**上，
异常往上抛的结果是 execution **反而永远停在等待态**（`runDetached` 兜底 catch 再调一次
`fail()` 还是同一个异常，只剩一行日志）。

受影响的不止新增代码：`resumeFromTask()` 里"节点不存在"、"input_submitted 不接受"、
`loadChecked()` 失败这三条**原本就会抛**。而"状态与任务对不上"这类内部不一致恰恰
最常发生在等待态 —— 最需要落 failed 的地方正好落不下去。

修法：新增 `exitWaitingIfNeeded()`，等待态先补一次 `→ resuming`，再走
`resuming → failed`（表里已有的边，语义也对："本来要恢复，恢复不了"）。

### 教训（已写进新 skill）
**"可判定" ≠ "判定正确"** —— 把不可判定换成判错只是换了个 bug。
指针字段记的是"被引用对象被创建过"，不是"它还没结束"；对账必须**解引用**。

### 验证
`tsc --noEmit`（src + test）✅ / `npm test` → **199/199** ✅（+3 用例：
续跑中途崩→补完、停机期间过期→收敛、任务不存在→failed）。

### 产出
- 新 skill `~/.workbuddy-ai/skills/durable-workflow-crash-windows/SKILL.md`
  （持久化工作流的崩溃窗口方法论：写序、可判定性分类、解引用对账、
  收尾路径必须能合法退出中间态、孤儿产物、重放安全、绑定校验、测试清单）。
- `docs/skill-flow.md` §17.6 拆出 17.6.1（引用要解引用）+ 17.6.2（收尾路径必须能退出等待态）。

---

## 第五轮：Skill Flow 四层分层（AST 正式化 + 控制流分析）

用户给了 `docs/sequence.md`（一份四层架构提案），本轮按它实现。

### 流水线（定稿）

```
SKILL.md → flow-parser.ts → FlowAst → flow-validator.ts → FlowDefinition → flow-analyzer.ts → issues
                                                                                  ↓
                                                                            WorkflowRunner
```

| 层 | 文件 | 输入 → 输出 | 回答 |
|---|---|---|---|
| 语法 | `skills/flow-ast.ts`（新） | — | 类型定义 |
| 语法 | `skills/flow-parser.ts` | Markdown → `FlowAst` | 作者**写了**什么 |
| 语义 | `skills/flow-validator.ts` | `FlowAst` → `FlowDefinition` | 允许执行什么 |
| 控制流 | `skills/flow-analyzer.ts`（新） | `FlowDefinition` → issues | 图的形状对不对 |
| 编排 | `workflow/runner.ts` | `FlowDefinition` → 推进 | 怎么跑 |

### 关键决策（与用户提案的三处偏离，都写进了注释）

1. **属性用有序数组，不用 `{role?: string; strategy?: 'ANY'|'ALL'}`**：
   ① 报错要指到属性自己那一行 → 行号必须随属性走；
   ② `'ANY'|'ALL'` **表达不了** `strategy: all` 这种"写了但不合法" —— 而用户自己
   说"AST 可以带 invalid role"。类型化 = 把合法性判定偷偷搬进语法层。
2. **`@flow` 用 `flows: FlowAstFlow[]` 而不是单个 `flow`**：作者可能写 0 个或 2 个，
   `flow-missing` / `flow-duplicate` 两条检查得报得出来。定成"必然恰好一个"等于让
   语法层替语义层做决定。
3. **route 带 `line`**（`FlowAstRoute {outcome, target, line}`）：重复出口的报错从
   "节点标题行"改成"重复的那一条自己的行"。AST 用作者视角的 `outcome/target`，
   `FlowDefinition` 用紧凑的 `on/to`，中间那次映射是两层的接缝。

### 新增：`FlowIssue.severity`

`severity?: 'error' | 'warning'`，**缺省 error**（新增检查默认拦住；反过来会让新检查静默失效）。
`hasBlockingIssue()` 统一判定。`FlowValidationResult.definition` 现在只在无 error 时给出。

### 新增：`flow-analyzer.ts` 三条检查（刻意互不重叠）

- `node-unreachable`（error）：从 start 走不到 → 永远不会被执行。
- `no-terminal-path`（error）：**可达**节点出发到不了任何终态 → 走进来就出不去。
  只查可达节点（不可达的已报过，叠一条是噪音）。
- `no-success-path`（warning）：**整条流程没有任何 `@end`** → 只可能以失败收尾。
  ⚠️ 第一版写成"start 到不了 @end"，与 `node-unreachable` **完全重叠**
  （`@end` 到不了 ⇔ 它不可达）→ 同一个问题说两遍。改成"有没有 @end 节点"后不重叠。
- **环本身不报错**（打回重做是常态）；要拦的是**没有出口的环**，恰好被 `no-terminal-path`
  覆盖 —— 用"活跃性"表达比"找环"更准，也少一个概念。
- 图就是 `Map<string, Set<string>>` 局部变量，**不建 `cfg.ts`**（用户明确反对 over-design）。
- 结构错误存在时**不跑分析器**：边指向不存在的节点时分析器会得出"这个节点出不去"
  这种**错误**结论。与编译器"先报语法再报类型"同一节奏。

### 新增：`flow:lint` CLI

`server/src/skills/flow-lint.ts` + `npm --prefix server run flow:lint -- <路径...>`。
- 用**与生产同一份**注册表（`flowRegistryLookup` / `businessRoleLookup`）与配置
  （`config.workflowAgentTools` / `workflowRequireAgentOutput`）—— lint 与运行时判定不一致，
  它给的"通过"就是假的。
- 输出 `path:line ERROR|WARN code  message`，按行号排序，有 error 时退出码 1。
- 没有 flow block 的普通技能**跳过**（不算错误）。
- 参数可以是 SKILL.md / 技能目录 / 技能根目录。

### 连带修的两处

- `workflow.test.ts` 的 `SPIN_MD`（自环 gate）原本没有任何终态出口 → 现在被
  `no-terminal-path` 静态拦下，测不到步数上限兜底。加一条 `stop -> done` + `@end done`，
  并把 `stop` 加进该 gate 的 `outcomes` 声明。
  **两条闸门分工**：静态分析器只能证明"图上没有出口"，证明不了"审核人永远不同意"。
- `flow-validator.test.ts` 的 `ok()` 改成跑完整流水线（校验 + 分析），新增 `semantics()`
  只跑语义层 —— 用于验证"校验器不做图分析"这条分层边界。

### 验证
`tsc --noEmit`（src + test）✅ / `npm test` → **219/219** ✅（199 → 219）
`npm --prefix server run flow:lint -- server/skills` → ✔ 通过（1 个 flow，2 个非 flow 跳过）

### 文档
`docs/skill-flow.md` 新增 §1.7「实现分层：三个模型，各答一个问题」：
四层流水线图、每层职责表、"为什么 AST 必须允许携带不合法内容"、
"为什么不再加一层"、severity 语义、三条控制流检查的互不重叠表、离线 lint 示例。

### 未动
`docs/sequence.md`（用户自己的提案草稿，未跟踪）—— 与实现有三处偏离（见上），
留给用户决定是否同步。

## Round 6：用户复核 `638a78b` 后指定的 4 个改动（已完成）

用户明确"只做这 4 个，其余保持不动"。

1. **P0 `flow-parser.ts` 不去重 route**。`seen` Set 破坏 AST 原则（AST 要忠实记录
   "作者写了什么"）。但不能直接删 —— parser 同时 visit 了 `paragraph` 和 `listItem`，
   `- success -> done` 会被收两次。**只收 `paragraph`** 后一条 route 天然只出现一次，
   于是删掉 `seen` / `key` / `if (seen.has(key)) continue`。
   重复的判定下沉到语义层。
2. **P0 `flow-validator.ts` 出口集合必须封闭**：新增 `node-unknown-outcome`（error，
   指 `route.line`），用现成的 `NODE_OUTCOMES[node.type]`。
   `@agent`/`@action` → success/fail，`@review` → approve/reject，`@stop`/`@end` → []，
   `@gate` → []（出口由注册表声明，另有 6c 检查）。
   不加这条的话 `- retry -> retry` 是**假分支**，而且分析器会把它当真边 → 可能误判可达。
3. **P1 `route-target-missing` 改指 `route.line`**（原来指 `node.headingLine`）。
4. **P1 `flow-lint.ts` skip bug**：`## @gates compliance` 产出 `block-unknown-type`
   但 `flows=[] nodes=[]`，lint 返回 `skipped` 把真错误吃掉。
   跳过条件补上 `&& !ast.issues.length`。

### 附带（同一次改动里顺手做掉）
- `route-outcome-duplicate` 拆成两级：同出口 + **不同**目标 → error（`-duplicate`）；
  同出口 + **相同**目标 → warning（新增 `route-outcome-redundant`，不阻断）。
- validator 头注释 13 件事 → **14 件事**，并写明"所有 route 相关问题都指 route 那一行"。
- 新增测试：`node-unknown-outcome` ×3（含"假分支可能让流程看起来可达"）、
  route 行号精确性 ×2、`route-outcome-redundant`、lint 块类型拼错不被跳过。

### 踩坑
`flow-validator.test.ts` 里改测试时把某个 `test(...)` 的开头一行删掉了，
留下孤儿 `const cases = [...]` → TS1128 `Unexpected "}"`。
教训：**Edit 替换 `test(` 开头行时要连 body 一起给全**，否则只换掉声明行会静默断链。
另一个：`node-unknown-outcome` 测试里的行号我按"含节点标题行"数成了 11，
实际 fixture 第 9 行才是 `- retry -> research` —— 数行号要照 `flow(...)` 的实参逐行数。

### 验证
`tsc --noEmit`（src + test）✅ / `npm test` → **224/224** ✅（219 → 224）
`flow:lint server/skills` → ✔ 通过（1 flow / 2 跳过）
用 `## @gates compliance` 探针跑 CLI → 正确报 `flow-missing` + `block-unknown-type`，exit 1

### 明确不做（用户主动推迟的设计缺口）
`strategy: ALL` 与 `ApprovalService` 的角色覆盖：`approval-policy.ts` 只判
`approvals.length >= required`，**不检查不同 business role 是否都出现过**。
现在的决定：**不扩展 ALL**；将来真需要多角色 ALL 时，在 `ApprovalService` 加 role coverage，
**不要**为此另设计一套 review DSL。

## Round 7：`@agent` 硬改名为 `@task`（已完成）

用户给了长提案：`@agent` 语义偏大 —— 它实际做的是"让当前 Agent Runtime 执行一次
受约束的 AI 工作单元"，而不是"起一个 agent / 委派 subagent"。改成 `@task` 之后
**Agent 不再是 Workflow 的一级概念**，只是 `@task` 的执行实现。

两个决策点（我提问，用户选）：
1. **skill 耦合**：原来 `@agent <id>` 的 id 必须命中技能目录（`skill-missing`）。
   用户选**去掉**。理由：和"节点 id 只是标签"直接冲突，`@task research`（技能叫
   `investment-research`）会校验不过。
2. **旧名兼容**：用户选**彻底删掉 `@agent`**（连带 `@subagent`），不做别名。
   理由：留别名等于把"起一个 agent"那个误读留在 DSL 里。

### 改动范围（29 个文件）
- **类型层** `workflow/types.ts`：`FlowNodeType` `'agent'` → `'task'`，
  `NODE_OUTCOMES` / `NODE_ATTRS` 同步；头注释写清"Agent 不是一级概念"。
- **parser** `flow-parser.ts`：`FLOW_HEADING` 去掉 `agent|subagent` 加 `task`；
  **删掉整个 `BLOCK_ALIASES` 机制**（不再需要别名表）。
- **validator** `flow-validator.ts`：删 `hasSkill` 选项 + `skill-missing` 检查；
  issue code `agent-tools-forbidden`/`agent-tools-widens`/`agent-output-missing`
  → `task-tools-*`/`task-output-missing`。头注释 14 件事 → 13 件事，
  并新增一段"刻意不检查 @task 的 id 是不是技能名"。
- **runtime/runner**：`runAgent()` → `runTask()`，`FlowAgentRun` → `FlowTaskRun`，
  `runAgentStep()` → `runTaskStep()`，`case 'agent'` → `case 'task'`，
  `REPLAY_SAFE_NODE_TYPES` 里的 `'agent'` → `'task'`。
  顺带删掉 step detail 里冗余的 `skill: node.id`（nodeId 已在，且现在叫 skill 是错的）。
- **definition-provider / flow-lint**：去掉 `hasSkill` wiring（`flow-lint` 的
  `LintOptions.hasSkill` 和 `defaultOptions` 里的那行也删了）。
- 内置示例 `server/skills/investment-research/SKILL.md`：`## @agent investment-research`
  → `## @task research`（**节点 id 也改**，让示例真正演示"id 只是标签"）。
- 5 个测试文件 + `skills/bpmn-to-flow/SKILL.md` + 6 个 doc。

### 刻意不改（部署面，改了会静默失配）
`COPILOT_WORKFLOW_AGENT_TOOLS` / `COPILOT_WORKFLOW_REQUIRE_AGENT_OUTPUT` 两个环境变量，
以及对应的 `config.workflowAgentTools` / `workflowRequireAgentOutput`、
`WORKFLOW_AGENT_ALLOWED_KINDS`、`opts.agentTools`。名字里的 AGENT 指的是
**Agent Runtime**（`@task` 的执行实现），不是被删掉的块关键字。已在
`types.ts` 的 `WORKFLOW_AGENT_ALLOWED_KINDS` 注释里写明这一点。

### 踩坑
- 批量替换文档时用占位符保护"历史沿革"那几行，结果占位符展开后又被二次替换，
  产出 "不做别名，不做别名" 这种重复串。**教训：占位符方案要么用不可能再被匹配的
  标记，要么干脆逐条精确替换，别做两轮。**
- `workflow.test.ts` 的 `LEGACY_MD` 原来是 `.replace('@agent', '@agent')`（空操作，
  因为上一轮批量替换把它自己改坏了）。这类"测试还在、但其实什么都没测"的情况，
  只有跑起来看它失败才会发现。

### 验证
`tsc --noEmit`（src + test）✅ / `npm test` → **224/224** ✅
`flow:lint skills` → ✔ 通过（1 flow / 2 跳过）
`## @agent w` 探针 → `block-unknown-type`，报错里列出可用类型含 `@task`

---

## Round 8：`@action` → `@command`（**平台级**改名，不只是 DSL）

同一个理由的第二次应用：`action` 也是"什么都能叫"的上位词，读的人会以为它涵盖
`@task` / `@gate` / `@review`。它实际是 DDD/CQRS 的 **Command** —— "流程提出业务意图，
服务端经 Policy / Approval 检查后执行一次受控的业务状态变更"。

四个执行词现在同层级：`@task` AI 做事 / `@gate` 系统判断 / `@review` 人做决定 /
`@command` 系统改变业务状态。`FlowNodeRuntime` = `runTask` / `runGate` / `openReview` / `runCommand`。

### 改名范围（用户选"连平台动作子系统一起改" + "硬改名，不做别名"）

- 目录：`src/actions/` → `src/commands/`（`action-registry.ts` → `command-registry.ts`，
  `action-service.ts` → `command-service.ts`）
- 标识符：`ActionService` / `ActionIntent` / `ActionPolicy` / `ActionExecutor` /
  `ActionExecutionContext` / `ActionClassification` / `ACTION_EXECUTORS` / `ApprovedActionResult`、
  `proposeAction` / `executeApprovedAction` / `hashAction` / `verifyActionHash` / `runAction` /
  `runActionStep` / `resumeAction` / `registerFlowAction` / `findFlowAction` / `FlowAction` /
  `flowActions` / `hasAction`，以及 `actionType` / `actionIntent` / `actionHash` / `actionSchema`
- DSL：`FlowNodeType` 的 `'action'` → `'command'`，`NODE_OUTCOMES.command` / `NODE_ATTRS.command`，
  `FLOW_HEADING` 正则，错误码 `agent-tools-*` 已在 R7 改为 `task-tools-*`
- DB：`action_intent` / `action_hash` → `command_intent` / `command_hash`
- 审计事件：`action.*` → `command.*`，`agent.proposed_action` → `agent.proposed_command`

### 刻意**不动**的四样（都是 wire format / 部署契约，改了会静默变坏）

1. **`commandHash` 的输入键**：`execution/hash.ts` 的 `frozenHashInput()` 里那个 key
   仍叫 `actionType`。hash 是对 key 排序后序列化的结果 —— 改键名 = 换 hash =
   所有在途审批在执行前复核时报"内容已被修改"。`hitl.test.ts` 加了**字面值断言**
   （`3533ae5e…`）钉住它。
2. **幂等键前缀**：`action:${executionId}:${commandHash}`。它离开进程边界传给下游网关，
   换前缀 = 部署期间在途重试去重失效、副作用执行两次。
3. **环境变量名**：`COPILOT_AUTO_APPROVE_ACTIONS` / `COPILOT_WORKFLOW_AGENT_TOOLS` /
   `COPILOT_WORKFLOW_REQUIRE_AGENT_OUTPUT` 及 config 字段名不变。
4. **已应用过的迁移**：`001_agent_execution.sql` 里的 `action_intent` / `action_hash` 保留原样，
   改名走**新增**的 `005_rename_action_to_command.sql`。

### 兼容层（每条都是"不加就 404/400，看起来像别的问题"）

- 路由：`POST /:id/commands`（新）+ `POST /:id/actions`（旧，指向**同一个** handler）
- 请求体：`commandSchema` 同时收 `commandType` 与 `actionType`，用 `.transform()` 归一化成
  `commandType`，下游只认新名
- 审计读取：`canonicalEventType()` 别名表，`GET /events` 在 API 边界把 `action.*` 归一化
- 人工任务 payload：`commandTypeOf()` 读 `p?.commandType ?? p?.actionType`
- 工作流标记：runner 里 `marker?.kind === 'action' ? 'command' : marker?.kind`
- MCP 工具名：`scripts/governance-mcp.mjs` 改名 `propose_command`，但 `tools/call`
  仍接受 `propose_action`（工具名写在 `COPILOT_MCP_SERVERS` 里，改名不会自动生效）
- 新增 `test/command-compat.test.ts` 锁住上面这些过渡期契约（文件头写明升级完可整文件删）

### 两个只有"跑起来"才能发现的坑

- **裸小写局部变量没被批量替换吃掉**：`runner.ts` 的 `const action` 被改成 `const command`，
  但 `if (action === 'waiting')` 三处没跟上 → `Cannot find name 'action'`。
  同类还有 `runtime.ts` 的 `const action = findFlowCommand(...)`、`execution-service.ts` 里
  整个 `runAction` 私有方法（`IDENT` 列表漏了裸 `runAction`）。
- **helper 插到了 class body 里**：`human-task-service.ts` 的模块级 `commandTypeOf()` 被
  按类内缩进插进去，触发一串 TS1068。

### schema 漂移测试要认识 `rename column`

`test/schema.test.ts` 原本只解析 `add column`，`SQLITE_COLUMN_UPGRADES` 里多了两条
`rename column` 后直接断言失败。改法：`ADD_COLUMN_RE` / `RENAME_COLUMN_RE` 拆成
**不带 `g`** 的单条解析版（避免 `lastIndex` 状态）+ 带 `g` 的扫描版；
`applyColumnChanges()` 先补列再改名算出 PG 的**生效**列集；新增断言"改名后的新名必须在
CREATE TABLE 里、旧名必须不在"，并让 SQLite↔PG 的升级集合比对同时覆盖补列与改名。

### 验证

`tsc --noEmit`（src + test）✅ / `npm test` → **229/229** ✅
`flow:lint skills` → ✔ 通过（1 flow / 2 跳过）
新增测试 5 个：`command-compat.test.ts` ×2（路由+字段别名、事件名归一化）、
hash 字面值冻结、`kind: 'action'` 旧标记仍能续跑、`@action` 报 `block-unknown-type`
